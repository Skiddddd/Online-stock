# backend/data

This directory contains the JSON database used by the Express backend.

- `db.json` is the runtime datastore.
- `backend/src/db.js` is the single source of truth for schema and initialization.
- Legacy PHP/Laravel database files were removed because this project does not run Laravel.
